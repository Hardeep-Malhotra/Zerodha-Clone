# Zerodha Clone

This repository contains a frontend-only clone of the Zerodha website.

## Project Structure

Zerodha Clone/
├── frontend/
│   ├── src/
│   ├── public/
│   ├── package.json
│   └── package-lock.json
└── README.md

## Frontend Setup

The React application lives inside the `frontend` folder.

### Steps to run the frontend

```bash
cd frontend
npm install
npm start
